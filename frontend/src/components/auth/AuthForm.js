import React from "react";
import styled from "styled-components";
import palette from "../../lib/styles/palette";

const AuthFormWrapper = styled.div``;
const AuthForm = () => {
  return <AuthFormWrapper></AuthFormWrapper>;
};

export default AuthForm;
